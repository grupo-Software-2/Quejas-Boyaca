spring.application.name=backend

# =========================================================
# ===  CONFIGURACIÓN LOCAL - NO SUBIR A GIT  ===============
# ===  (USA LAS CREDENCIALES DEL GRUPO PARA CORREO)  ========
# =========================================================

# Conexión PostgreSQL Local
spring.datasource.url=jdbc:postgresql://localhost:5432/quejas_db
spring.datasource.username=postgres
spring.datasource.password=123
spring.datasource.driver-class-name=org.postgresql.Driver

# Propiedades del RECAPTCHA (Valor de prueba)
google.recaptcha.secret=local-test-key
google.recaptcha.verify-url=https://www.google.com/recaptcha/api/siteverify

# HABILITAMOS EL CORREO CON LAS CREDENCIALES REALES DEL GRUPO
notification.email.enabled=true
notification.email.admin-emails=pruebassoftware552@gmail.com
notification.email.from=pruebassoftware552@gmail.com
notification.email.from-name=Sistema de Quejas Boyaca

# SMTP Configuration (Usando Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=pruebassoftware552@gmail.com
spring.mail.password=sadfqcugctwhbtxr
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


# =========================================================
# ===  PROPIEDADES COMPARTIDAS (El resto del archivo)  ======
# =========================================================

# Pool de conexiones HikariCP
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.max-lifetime=1800000

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Nueva contraseña de eliminación, necesaria aunque no la uses
admin.delete.password=test_password_local